import styled from "styled-components";

const DimLayout = styled.div`
  width: 100vw;
  height: 100vh;
  background: rgba(0, 0, 0, 0.5);
  z-index: 55555;
  position: absolute;
  top: 0;
  left: 0;
`;
export { DimLayout };
